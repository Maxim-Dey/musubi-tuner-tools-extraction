# Quickstart: Later Remote Verification

This page is a run order for later Preparation, Verification, and Report phases. Stage 5.2 only writes local plans; no dependency installation, cache creation, or model run has occurred.

1. Recheck the selected Pod, host key, free resources, `/workspace` models/data, and whether Basic SSH can reliably transfer a canary and retrieve evidence. Use the provided interactive SSH identity without printing or copying its private key. If PTY transfer cannot be verified, request exposed-TCP SSH before any model test.
2. Assign one UTC `run_id`; create `/workspace/musubi-val-loss-tests/<run_id>/` once. Fetch exact commit `e8af43abc32d967153e3aa3c31d50c77d3f7c47e` from branch `001-scope-qwen-image-lora`, transfer the finished local overlay as an archive, and verify archive and per-file hashes. The remote default HEAD is a different commit.
3. Create an isolated Python 3.12 `.venv`; install the project's declared dependencies, CUDA 12.8 torch 2.8.0/torchvision 0.23.0, TensorBoard, and pytest only there. Record versions and imports. Run root scripts with `PYTHONPATH=$R/code/src` because editable installation currently references an absent README. Do not redownload models.
4. Select existing train/familiar/unfamiliar images, prove role membership by hashes, copy image/caption pairs into `fixtures/`, and record source/copy hashes. Build both latent and text caches through the existing Qwen cache commands. Prepare G03 renamed/reordered source copies with their own valid caches and manifest. Freeze canonical and G03 source/cache bindings before any validation.
5. Materialize each opt-in `$R/experiments/<case>/train.toml` plus its train/validation dataset TOMLs from the Stage 4 example. Make G05-off an isolated legacy config without `experiment_dir` or `val_dataset_config`, and disable samples there. Preserve BF16, 1024, SDPA, AdamW8bit, LoRA 16/16 and FP32 sole adapter. Apply only the case-specific short budgets, two-step warmup, validation protocol and fixed four-step sample where enabled. Inspect **effective** parsed settings before any optimizer update; reject a 1,600-step fallback.
6. Run the exact bounded [G01–G10 protocol](verification-plan.md) through its one-shot launcher. Keep source/cache gates first; G03/G04 use fixed weights without updates, G05 runs branches sequentially, G06-current copies both its real current state and prior best into the isolated experiment while G06-best copies its real best, and G09 runs only if the same Pod has two suitable GPUs. After an SSH drop, inspect PID, exit, logs and outputs before deciding whether anything needs action.
7. Retrieve concise manifests, logs, event summaries and metadata; leave bulky models/states in the unique remote tree. Write local `verification.md` and `verification.json` with every G status, actual counts/differences and evidence location. Reconcile README and ensure no agent test process still updates weights; leave the Pod and user processes running.
